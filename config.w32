// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("escaper", "for escaper support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("escaper", "enable escaper support", "no");

if (PHP_ESCAPER != "no") {
	EXTENSION("escaper", "escaper.c");
}

